import { ProfilePhotoPipe } from './profile-photo.pipe';

describe('ProfilePhotoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfilePhotoPipe();
    expect(pipe).toBeTruthy();
  });
});
